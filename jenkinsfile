pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git(
                    git branch: 'dev-build', url: 'https://github.com/Prathm0025/Slot-Vikings-dev.git'
                )
            }
        }
}
}
